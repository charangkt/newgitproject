{
  "name": "task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Task Manager - TypeScript frontend + PHP REST API",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "php -S localhost:8000 -t public",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
